version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      #- DATABASE_URL="" # sourced from the git action. uncomment and populate if building docker locally
    restart: unless-stopped
